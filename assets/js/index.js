// const tasks = document.createElement('div');

//add get request to db here on click 
// trying to add an event listener to the "Get tasks" button so that it makes a get request to the server 
document.getElementById('retrieve').addEventListener('click', () => {
    console.log('CLICKED')
    // fetch('/api')
    //   .then((response) => response.json())
    //   .then((data) => {
    //       console.log(data.rows)
    //   })
})

// document.getElementById('task-list').appendChild(tasks)